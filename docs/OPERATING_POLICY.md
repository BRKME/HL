# Операционная политика проекта

Как проект живёт между сессиями: что можно менять без чекпойнта, что нельзя,
и по каким данным принимаются решения. Дополняет узкие документы:
[HIERARCHY_POLICY.md](HIERARCHY_POLICY.md) (стратегия → тактика),
[KPI_SYSTEM.md](KPI_SYSTEM.md), [PLAN_UNFREEZE_20_07.md](PLAN_UNFREEZE_20_07.md).

Заведён 03.08.2026. Менять только явным решением оператора.

---

## 0. Протокол работы с этим документом

Зафиксировано решением оператора 03.08.2026.

**Перед любыми изменениями в проекте — прочитать этот документ.** Он не
справочник для сверки постфактум, а вход в работу: половина правил ниже
выведена из ошибок, которые иначе повторяются.

**Перед каждым пушем — `python3 scripts/preflight.py`.** Требование
оператора от 28.08.2026: проверять всё комплексно после любых изменений.
Сделано командой, а не памяткой, по уроку августа: из 15 дефектов месяца 6
внёс я сам, четыре из них — в воркфлоу, и одну и ту же ошибку с
некоммитящимся маркером я повторил трижды, причём второй раз через две
недели после того, как сам записал правило в этот документ. Что не
держится вниманием, проверяется машиной.

Preflight проверяет: тесты зелёные ДВАЖДЫ подряд (ловит утечки состояния
между прогонами), боевой `state/` не изменён прогоном, контракт «код пишет
файл — воркфлоу его коммитит», импорт всех модулей, отсутствие секретов в
дереве, здоровье журнала. Первые пять блокируют пуш; здоровье журнала
предупреждает, потому что журнал бывает несвежим по внешним причинам.

Сам preflight проверен воспроизведением двух настоящих поломок: убранный
`git add` маркера и тест, пишущий в боевой `state/`. Обе поймал с внятным
диагнозом. Проверка, не проверенная на реальном дефекте, — ещё не проверка.

**Изменения вносятся в документ тем же коммитом, что и в код.** Разошедшаяся
с кодом политика хуже отсутствующей — ей верят, и она врёт. Если правило
оказалось неверным, оно правится, а не игнорируется молча.

**Что обязано попадать сюда:**
- новое правило или отмена старого;
- решение, принятое по данным (с цифрой, на которой оно принято);
- обнаруженный класс ошибки — в тот раздел, который его предотвращает;
- закрытие или сдвиг чекпойнта;
- открытый вопрос, оставленный на решение оператора.

**Что не попадает:** рутинные правки, не меняющие правил. Журнал изменений в
конце ведётся по сессиям, а не по коммитам.

---

## 1. Заморозка и чекпойнты

**Принцип.** Между чекпойнтами тактическая логика не трогается. Хотелки идут
в бэклог. Если что-то сломалось шумно — бот молчит, ошибки в Actions — об
этом сообщается немедленно, не дожидаясь даты.

**Что заморожено:** логика вердикта, пороги, веса, правила выхода. Любое
изменение здесь обнуляет сравнимость накопленной выборки.

**Что разрешено всегда:** исправление багов, наблюдаемость, подача. Ключевое
различие — **логика против подачи**. Показать то, что система уже посчитала,
можно в любой момент: выборка не меняется, потому что не меняются числа.
Изменить то, что она считает — только на чекпойнте.

Практический пример 03.08: инвариант иерархии гасил шорты и отдавал наружу
`WAIT`. Соблазн был снять инвариант. Вместо этого показали погашенный
сигнал — оператор получил информацию, выборка осталась чистой.

**Пороги открытия темы.** Задача активируется, только если её собственные
ворота открылись на данных. `zone_strength` закрыта 02.08 именно так:
`sl_breach` 2.2% против порога 20–25%.

**Ближайший чекпойнт:** следующий назначает оператор; H2 возвращается при
n≥80 закрытых. Прошедший — 23.08.2026 — статистика выходов на
`policy_version=2` при n≥20, включая предзарегистрированные гипотезы H1
(дребезг режима) и H2 (окупаемость тактического слоя), см. §3.

---

## 2. Слои решения

Вердикт проходит два слоя последовательно.

| слой | что смотрит | где |
|---|---|---|
| **график** | EMA50/EMA200, перекупленность, перепроданность — только сама монета | `_raw_decision()` |
| **режим** | BULL/BEAR и фаза цикла от OracAI, может отменить решение графика | `_apply_regime()` + `enforce_hierarchy()` |

Результат первого — `verdict_raw`, второго — `verdict`. Оба пишутся в журнал
для A/B: добавляет ли режимный слой преимущество.

**Что показали данные на 03.08** (594 записи с обоими слоями):

| монета | raw LONG | raw SHORT | fin LONG | fin SHORT |
|---|---|---|---|---|
| BTC | 0 | 58 | 31 | 38 |
| ETH | 0 | 70 | 27 | 50 |
| HYPE | 99 | 0 | 38 | 0 |
| NEAR | 53 | 0 | 28 | 0 |

По BTC и ETH график не выдал ни одного лонга — все 58 финальных лонгов
создал режимный слой. И 60 сырых шортов погашены в `WAIT`, все при
`regime=BULL`. По HYPE и NEAR блокировки нет вообще: график просто ни разу
не увидел нисходящего тренда.

Вывод для чекпойнта: режимный слой делает большую часть работы по мейджорам,
и его цена измерима с обеих сторон — спасённые шорты против упущенных.

---

## 3. Политика выходов

**Гистерезис `verdict_flip`, K=3** (`FLIP_CONFIRM_RUNS`, введён 02.08).
Выход по расхождению вердикта требует трёх подряд прогонов не на стороне
позиции — гвард ходит раз в 2 часа, то есть около 6 часов подтверждения.

Калибровка: длины серий `WAIT` бимодальны — шум 1–2 тика, реальные развороты
6+ тиков, между ними разрыв. K=3 берётся бесплатно.

**Исключения из гистерезиса:**
- разворот (позиция LONG, вердикт SHORT) — выход немедленный;
- `sl_breach` — гистерезис не применяется;
- `regime_flip` — переоценивается даже при висящем flip.

Каждая запись выхода несёт `policy_version`. Старая политика — 45 выходов,
93% из них `verdict_flip`, 18 из 42 с перевходом внутри 6 часов. Ожидание от
новой: доля `verdict_flip` падает, перевходы стремятся к нулю.

### Предзарегистрированная гипотеза H1 — дребезг режима

Зарегистрирована 08.08.2026, до сбора данных. Проверяется на чекпойнте
23.08 вместе с основной статистикой. Записана заранее именно для того,
чтобы критерии нельзя было подогнать под результат.

**Наблюдение.** Гистерезис снял дребезг там, куда его ставили, и не снял
там, куда не ставили. На 08.08, при n=7:

| доля выходов | policy 1 (n=45) | policy 2 (n=7) |
|---|---|---|
| `verdict_flip` | 93% | 57% |
| `regime_flip` | 4% | 43% |

Режим за 14 дней перевернулся пять раз: BULL → BEAR → BULL → BEAR → BULL →
BEAR. `regime_flip` освобождён от гистерезиса намеренно — смена
стратегического режима считалась редким и весомым событием. Пять смен за
две недели этому допущению противоречат.

**Гипотеза.** Дребезг не устранён, а переехал в канал без защиты.

**Что меряем при n≥20 на `policy_version=2`:**

1. доля `regime_flip` среди выходов;
2. медиана R выходов по `regime_flip` отдельно от `verdict_flip`;
3. доля повторных входов в ту же сторону по той же монете в течение 24 ч
   после выхода по `regime_flip`;
4. число смен режима за 30 дней.

**Решающие правила, зафиксированные заранее:**

- Доля `regime_flip` ≥ 30% **и** повторных входов внутри 24 ч ≥ 40% →
  дребезг подтверждён, ставим подтверждение смены режима (кандидат:
  K=2 подряд суточных снимка) по образцу `FLIP_CONFIRM_RUNS`.
- Медиана R по `regime_flip` ≥ 0 → выходы защитные, не трогаем, даже если
  частые. Частота сама по себе не дефект; дефект — частота, стоящая денег.
- Доля `regime_flip` < 30% → гипотеза не подтвердилась, закрываем и не
  возвращаемся без новых данных.
- Смен режима за 30 дней ≤ 3 → выборка 08.08 была всплеском, продлеваем
  наблюдение до следующего чекпойнта без изменений.

**Чего не делаем в любом случае:** не трогаем сам детектор режима в
OracAI. Здесь проверяется реакция на смену, а не правильность её
определения — это разные вопросы и разные выборки.

**Считается скриптом:** `python3 scripts/h1_checkpoint.py`. Пороги живут в
`src/h1_metrics.py` константами. Менять их после того, как увидели
результат, — значит отменить смысл предрегистрации: если данные заставят
пересмотреть критерии, это новая гипотеза в этом документе, а не правка
тех же чисел.

Прогон 08.08 (n=7, до срока): доля `regime_flip` 43%, возвратов внутри
24 ч — 0%, медиана R −0.035, смен режима за 30 дней 4. Вердикт скрипта —
«недостаточно данных». Нулевой возврат при высокой доле стоит отметить
заранее: если он таким и останется, гипотеза не подтвердится по третьему
правилу, и это будет означать, что `regime_flip` закрывает позиции
всерьёз, а не дёргает их.

### Предзарегистрированная гипотеза H2 — окупается ли тактический слой

Зарегистрирована 09.08.2026 по решению оператора, до сбора данных.
Проверяется на чекпойнте 23.08 вместе с H1.

**Наблюдение.** Недельные KPI 09.08:

| | значение | порог |
|---|---|---|
| WR тактики | 20% (n=15 закрытых на неделе) | — |
| avg R | −0.16 (по 66 оценённым) | — |
| LONG avg R | −0.10 (n=47) | — |
| SHORT avg R | −0.32 (n=19) | — |
| ворота денег (LONG) | −0.10 · медиана −0.28 | ≥ +0.4 · > 0 |

Ворота денег закрыты и близко не подходят к порогу. При этом субботний
недельный планнер даёт **+20.3 п.п. против always-DCA** на 11 неделях. То
есть слой, работающий на недельном горизонте, преимущество даёт, а слой,
работающий на дневном, — нет.

**Гипотеза.** Тактический слой не окупает своей сложности: он генерирует
выходы, требует гистерезиса, ест внимание оператора и при этом не
добавляет доходности.

**Что меряем при n≥40 закрытых с оценкой R:**

1. avg R и медиана R по всем закрытым, отдельно LONG и SHORT;
2. долю доверительного интервала avg R, накрывающую ноль (bootstrap);
3. те же метрики отдельно для сделок, где тактика согласна с недельным
   планнером, и где расходится;
4. альфу планнера за тот же период — как базу сравнения.

**Решающие правила, зафиксированные заранее:**

- Медиана R ≥ 0 **и** доверительный интервал avg R накрывает ноль →
  слой не вредит, но и не доказал пользы: оставляем, возвращаемся при n≥80.
- Медиана R < 0 **и** верхняя граница интервала avg R < 0 → слой
  систематически теряет: отключить эмиссию тактических входов, оставить
  только выходы по политике. Это не удаление кода, а перевод в наблюдение.
- Расхождение с планнером даёт R хуже согласия минимум на 0.2 → проблема
  не в тактике как таковой, а в её праве идти против недельного слоя:
  ограничить тактику согласными сделками, а не выключать.
- SHORT-сторона хуже LONG минимум на 0.2 при n≥15 на сторону → сузить
  тактику до LONG, SHORT вернуть в наблюдение.

**Считается тем же скриптом:** `python3 scripts/h1_checkpoint.py` печатает
обе гипотезы. Прогон 09.08 (n=53, до срока): avg R −0.055, медиана −0.040,
LONG −0.047 (n=41), SHORT −0.082 (n=12), 95% интервал [−0.154, +0.034].
Вердикт — «не доказано»: интервал накрывает ноль. То есть недельные KPI с
их WR 20% рисуют картину мрачнее, чем выдерживает статистика: на всей
истории слой не выигрывает, но и систематического проигрыша пока не видно.

**Чего не делаем:** не смешиваем с H1. H1 про то, как выходим; H2 про то,
стоит ли входить. Совпадение чекпойнта не повод объединять выводы —
если обе подтвердятся, порядок действий определяет оператор.

### Предзарегистрированная гипотеза H3 — сузить универс до BTC

Зарегистрирована 13.08.2026 по предложению оператора, до сбора данных.
Проверяется на чекпойнте 23.08.

**Наблюдение.** Режимный слой берёт вердикт из OracAI, а OracAI обучен на
BTC. Его вердикт применяется ко всем девяти монетам универса — то есть за
пределами данных, на которых модель откалибрована. Таблица §2 (n=594)
показывает, что вклад слоя распределён крайне неравномерно: по BTC и ETH
график не выдал ни одного лонга, все 58 финальных лонгов создал режимный
слой; по HYPE и NEAR блокировок нет вообще — график ни разу не увидел
нисходящего тренда, и слой фактически не работает.

**Гипотеза.** Преимущество режимного слоя сосредоточено на мейджорах и
максимально на BTC, а на альтах слой либо молчит, либо экстраполируется без
основания. Тогда сужение универса повышает качество сигнала.

**Что меряем при n≥20 на `policy_version=2`:**

1. avg R и медиана R по BTC отдельно против остальных монет;
2. долю сделок, где режимный слой изменил `verdict_raw`, по каждой монете;
3. те же метрики для мейджоров (BTC, ETH) против альтов;
4. корреляцию R с тем, был ли вердикт создан слоем или графиком.

**Решающие правила, зафиксированные заранее:**

- BTC даёт медиану R выше остальных минимум на 0.2 при n≥15 по BTC →
  сузить универс до BTC (+ETH, если мейджоры неотличимы между собой).
- BTC неотличим от остальных (интервалы пересекаются) → универс не трогать:
  гипотеза о «модели вне своих данных» не подтвердилась на результате.
- Альты дают R лучше BTC → гипотеза опровергнута, слой полезен шире, чем
  предполагалось; вернуться к вопросу не раньше n≥80.

**Плечо в эту гипотезу не входит.** Оператор предлагал сузить универс и
одновременно поднять плечо. Это два независимых решения: универс — про
качество сигнала, плечо — про размер риска, и из первого не следует второе.
Концентрация к тому же убирает диверсификацию, то есть повышает волатильность
счёта сама по себе. Размер обсуждается только после того, как H2 покажет
окупаемость слоя; пока «на всей истории слой не выигрывает» (§3), увеличение
плеча было бы ставкой на неподтверждённый edge.

### 02.09.2026 — единственное неизмеренное: торговля оператора

Возражение оператора на HLP справедливо: 15–25% годовых при просадках
5–12% и риске скоординированных атак — плохая сделка, столько дают и без
крипто-риска. И глубже: при счёте $190 никакая доходность не имеет смысла.
**Связывающее ограничение — капитал, а не стратегия.**

Но одно мы не измерили ни разу. Два дня доказывали, что модель не
обгоняет случайный вход, а за тот же период оператор сделал руками
**+46.8%** — и это единственный источник преимущества, который мы вообще
наблюдали.

Заведён `src/operator_edge.py` и воркфлоу `operator-edge`: сделки
восстанавливаются из исполнений (частичные закрытия и доливки сводятся в
одну сделку), считаются WR, средняя и медианная доходность, PnL по
монетам, среднее удержание.

**Та же строгость, что к модели**, включая порог: меньше 30 сделок —
«НЕЛЬЗЯ СУДИТЬ», без вдохновляющих формулировок. И положительный
результат сам по себе выводом не считается: нужен контроль случайным
входом, как и для модели. Двойного стандарта — строгость к своей работе,
снисходительность к чужой — быть не должно.

Ограничение известно заранее: сделок, скорее всего, окажется мало, и
тогда честный ответ будет «судить нельзя».

### 02.09.2026 — шорт после роста: первое настоящее преимущество

Прогон (+50% за 200 дней, девять монет, 2000 дней истории):

| горизонт | шорт | случайный шорт | обгон | худшая |
|---|---|---|---|---|
| 3 дня | +0.23% | +0.56% | −0.33 | −29% |
| 7 дней | +2.18% | +0.62% | +1.56 | −56% |
| 14 дней | +3.55% | +1.43% | +2.13 | −75% |
| **30 дней** | **+11.80%** | +3.70% | **+8.10** | **−83%** |

Обгон растёт МОНОТОННО с горизонтом при выборке 171–179 событий. Это
структура, а не шум: случайные величины так себя не ведут. Контроль
пройден — сравнение со случайным шортом, который сам по себе прибылен.

**Но критерий риска сработал: худшая сделка −83% при пределе −40%.** При
плече 5× ликвидация наступает около −20%, то есть часть сделок уносит весь
счёт. Преимущество и катастрофа растут вместе: −29, −56, −75, −83.

**Моделирование перед замером** (по требованию оператора). Моё опасение
было: преимущество берётся из выживания в продолжении роста, значит стоп
выбьет до разворота. Модель, откалиброванная на наблюдаемые числа, дала
обратное — средняя при ужесточении стопа слегка РАСТЁТ (+8.11% без стопа
против +8.76% при −15%), медиана почти не меняется. Стоп режет левый хвост,
не трогая типичную сделку.

Но это свойство МОЕЙ модели, а не рынка: траектории после сигнала я задал
сам. Проверять надо на настоящих путях.

Добавлена сетка стопов с шагом: 5, 8, 10, 12, 15, 18, 20, 25%. Верхняя
граница — 25%, потому что при плече 5× ликвидация около 20% и стоп шире
неё бессмыслен. Стоп проверяется ПО МАКСИМУМАМ, а не по закрытиям: цена
может выбить его внутри дня и вернуться, и считать это выживанием значило
бы завышать результат.

Вердикт теперь выбирает лучший вариант ТОЛЬКО среди переживающих предел
риска: преимущество при неограниченном убытке не годится, счёт кончится
раньше, чем стратегия отработает.

**Оговорка по настройке:** окно 200 дней — это уже не памп, а затяжной
рост. Замер показал возврат к среднему после долгого подъёма, а не откат
после резкого выброса. Это не хуже, но называть надо своим именем.

## ИТОГ ИССЛЕДОВАНИЯ 01–02.09.2026: направленного преимущества нет

Полный прогон на 2000 днях, девять монет, со всеми проверками:

| измерение | результат |
|---|---|
| модель против случайного входа | **+0.959 против +1.459** — модель ХУЖЕ |
| после издержек | +0.852, цена 0.107 R — «издержки съедают всё» |
| скользящая проверка | 59% плюсовых при пороге 60% |
| фильтр по средней | **отрицателен во всех 6 конфигурациях** |
| просадка | −9.34 против −37.69 у удержания |
| доход/просадка на отрезках | лучше удержания в 60% |

**Решающее: модель проигрывает случайному входу с той же экспозицией.**
Раньше она его обгоняла, но лучший набор на 2000 днях сменился с «окно 60»
на «окно 40», и новый проигрывает. Сама нестабильность выбора — признак
отсутствия преимущества: устойчивый эффект не переезжает при добавлении
ста дней.

**Идея режимного фильтра проверена и не подтвердилась.** Все шесть
комбинаций длины средней и зоны нечувствительности дали отрицательный
прирост, помогая лишь в 6–21% наборов. MA200 при этом режет две трети
сделок — дорогая цена за ухудшение.

**Что перебрано и закрыто за два дня:** входы EMA+RSI (416 сделок), четыре
варианта выхода, относительная сила (H4), 140 конфигураций моментума на
трёх горизонтах истории, режимный фильтр OracAI, режимный фильтр по
средней. Ни одно не дало преимущества, переживающего контроль случайным
входом и издержки.

**Единственное, что уцелело:** просадка вчетверо меньше удержания. Это
структурное свойство — модель вне рынка половину времени, — а не умение
предсказывать.

**Вывод.** Система не является источником направленного преимущества, и
дальнейший перебор на этих данных даст только подгонку. Ценность
инструмента — в расчёте риска, дисциплине исполнения и наблюдаемости.
Задокументированное преимущество в крипте (фандинг-арбитраж, ~19% годовых
при просадке менее 2%) требует капитала от $5 000 и другой природы: доход
из структуры рынка, а не из угадывания направления.

### 02.09.2026 — раннеры теперь исполняются тестами

Перебор упал в Actions: `UnboundLocalError: best_p`. Я вставил блок
издержек ДО места, где переменная определяется. Preflight не поймал —
тесты покрывают `src/`, а скрипты только разбирались синтаксически.
Синтаксис был верен, порядок вычислений — нет.

Заведён `tests/test_runner_smoke.py`: все три раннера исполняются целиком
на подставных данных — сеть и Telegram заглушены, история синтетическая.
Проверяется не результат, а то, что скрипт доходит до конца.

Тест немедленно окупился: после переноса блока он поймал ВТОРУЮ такую же
ошибку (`rand_avg` использовался до вычисления) — уже в песочнице, а не в
Actions.

**Класс ошибки узнаваем:** правка вставлена не в то место, всё выглядит
корректно, ломается только при исполнении. Так же были свечи в один
построитель из двух и маркер в один `git add` из двух. Разница в том, что
теперь машина проверяет исполнение, а не только форму.

### 02.09.2026 — издержки: они решают знак

В бэктесте издержек не было вовсе. Пока преимущество было отрицательным,
это не мешало — «не работает без издержек, с ними тем более». Теперь мы у
грани, где они решают.

| удержание | комиссия | фандинг | всего |
|---|---|---|---|
| 5 дн | 0.09% | 0.15% | 0.24% |
| 10 дн | 0.09% | 0.30% | 0.39% |
| 20 дн | 0.09% | 0.60% | **0.69%** |

Лучший набор из перебора держит 20 дней. В единицах R (суточная
волатильность альтов 4–5%) это **0.24…0.30 R за сделку**, тогда как
измеренное преимущество против случайного входа — **+0.44…+0.78 R**.
Издержки съедают от трети до двух третей.

Знак фандинга важен отдельно: лонг ПЛАТИТ при положительной ставке. Все
сигналы за месяц были лонгами при ставке +11% — система систематически
вставала на сторону, которая платит.

Издержки выключены по умолчанию: прежние прогоны должны остаться
сравнимыми, иначе сравнение «до и после» станет невозможным молча. В
отчёт добавлена строка «до издержек / после / цена» и явное «издержки
съедают всё преимущество», когда чистый результат опускается ниже
случайного входа.

### 02.09.2026 — версии действий подняты до Node 24

Actions предупредил о выводе Node 20: `checkout@v4` и `setup-python@v5`
работают на нём. Прогоны не падают — раннер подставляет Node 24, — но
когда поддержку снимут, упадут ВСЕ шестнадцать воркфлоу разом, включая
сбор данных. Такой отказ дорог не сам по себе, а тем, что происходит
одновременно везде.

Проверено по `action.yml`: `checkout@v5` и `setup-python@v6` объявляют
`using: node24`. Взяты они, а не самые новые (v6/v7): нужен минимум, где
проблема снята, а не последняя версия ради последней.

Заведён тест: версии не ниже минимума и ЕДИНЫ во всех воркфлоу. Разъезд
версий — тот же класс ошибки, что уже случался трижды за неделю (свечи в
один построитель из двух, маркер в один `git add` из двух).

### 02.09.2026 — режимный фильтр: OracAI не подошёл, средняя проверяется

**Идея оператора:** использовать OracAI, чтобы торговать только в растущем
рынке. Проверено на журнале за три месяца (1502 записи с ценой и режимом):

| режим | форвард 5 дн | форвард 10 дн |
|---|---|---|
| BULL | +3.12% | +3.00% |
| TRANSITION | +3.91% | +3.86% |
| **BEAR** | +2.27% | **+8.27%** |

На десятидневном горизонте после BEAR доходность ВЫШЕ, чем после BULL, на
5.27 п.п. По медианам разницы нет вовсе. Доводов «за» нет ни одного.

Оговорки: три месяца, один цикл, наблюдения перекрываются (одна монета в
соседние дни — не независимые точки), период растущий. Честная формулировка
— данные не подтверждают идею и не опровергают её убедительно.

**Мы это уже делаем, и цена известна.** Инвариант иерархии блокирует лонги
при BEAR; он же погасил 23 из 26 лонгов за рост 18–21.08. Детектор меняет
мнение семь раз в месяц и не заметил движения в 20–29% — такой инструмент
фильтром служить не может.

**Принципиальное ограничение:** история режимов OracAI существует только в
нашем журнале за три месяца. Прогнать её по 1900 дням невозможно, значит
эту гипотезу нельзя проверить на том же горизонте, что и всё остальное.

**Что делаем вместо.** Фильтр по скользящей средней: восстановим по любой
истории, устойчив, проверяем на тех же 1900 днях. Оператор добавил важное:
у самой линии случаются ложные пробои. Реализована зона нечувствительности
— внутри неё состояние НАСЛЕДУЕТСЯ, чтобы сменить режим, цене надо уйти за
границу зоны. Это тот же гистерезис, что мы применили к `verdict_flip`.

**Сравнение парное, а не перебором.** Тот же набор параметров с фильтром и
без; улучшение усредняется по ВСЕМ наборам и печатается доля наборов, где
фильтр помог. Искать лучший из ещё большего перебора — ловушка, к которой
мы уже дважды подходили вплотную.

### 02.09.2026 — скользящая проверка и конфликт двух измерений

| прогон | вперёд | плюсовых отрезков | доход/просадка (один сплит) |
|---|---|---|---|
| 9 монет | +0.270 | **44%** (32 отрезка) | 2.62 против 0.45 |
| 2 монеты | +0.677 | **38%** (8 отрезков) | 0.64 против −0.33 |

**По моему собственному критерию, записанному до прогона (порог 60%
плюсовых отрезков), модель НЕ ПРОХОДИТ.** Средний результат вперёд
положителен, но большинство периодов убыточны: подпись «немного крупных
выигрышей тянут среднее». Оценка значимости: t ≈ 1.5 и 1.9 — не
установлено.

**Но два измерения конфликтуют, и замять это нельзя.** На одном сплите
доход на единицу просадки у модели в 5.8 раза лучше, чем у удержания. На
скользящих отрезках доходность неустойчива.

Разрешение: преимущество по просадке **структурное** — модель вне рынка
половину времени и потому просаживается меньше независимо от того, угадала
ли она направление. Оно может держаться там, где доходность не держится.
Это разные утверждения, и мерить их надо порознь.

Добавлено: доход на единицу просадки считается на КАЖДОМ отрезке
скользящей проверки, печатается доля отрезков, где модель по этому
показателю лучше удержания. Если она высока при 44% плюсовых по
доходности — значит ценность модели в снижении риска, а не в предсказании,
и это надо называть своим именем.

### 02.09.2026 — просадка переворачивает картину

| прогон | модель/сделку | случайный | умение | просадка модели | просадка удержания |
|---|---|---|---|---|---|
| 9 монет | +2.104 | +1.329 | **+0.775** | **−5.46** | −14.69 |
| 2 монеты | +0.193 | −0.092 | **+0.285** | **−5.18** | −17.35 |

**Просадка модели втрое меньше**, при пребывании в рынке 40–47% времени.
Для оператора на плече 5× это важнее доходности: вдвое меньшая просадка
допускает вдвое большее плечо при том же риске.

Оценка дохода на единицу просадки (9 монет): модель ≈ +2.45 против +0.45 у
удержания. Точный итог теперь печатается — раньше в отчёт попадала только
просадка, а сумму приходилось оценивать по числу сделок.

**Добавлена скользящая проверка** — главный тест на устойчивость. Один
сплит 70/30 отвечает, работало ли это в конце периода; скользящая
отвечает, работало ли ПОВТОРЯЕМО: набор выбирается по прошлому, меряется
на следующем отрезке, и так четыре раза. Если лучший набор скачет от
отрезка к отрезку, а результаты вперёд около нуля — устойчивого
преимущества нет, каким бы удачным ни был один сплит. Вердикт учитывает и
средний результат вперёд, и долю плюсовых отрезков.

### 01.09.2026 — умение есть, но я сравнивал несравнимое

С правильным контролем модель обгоняет случайный вход **на обоих наборах**:

| прогон | модель | случайный вход | умение | в рынке |
|---|---|---|---|---|
| 9 монет | +2.067 | +1.229 | **+0.838** | 46% |
| 2 монеты | +0.148 | −0.138 | **+0.286** | 40% |

Обе величины — на сделку, сравнение корректно. Это первый честный
положительный сигнал за всё исследование: **умение выбирать момент есть**,
и оно устояло на растущем и на падающем рынке.

**Но строку «купи и держи дал больше» я напечатал зря.** `buy_and_hold_r`
возвращает итог ЗА ПЕРИОД, а `avg_r` — среднее НА СДЕЛКУ. Сравнивать их
нельзя, вывод был бессмысленным. Исправлено: считается суммарный R модели
за период.

**Добавлена просадка — при плече 5× она важнее доходности.** Стратегия с
меньшей доходностью и вдвое меньшей просадкой допускает вдвое большее
плечо при том же риске, то есть даёт БОЛЬШЕ денег. Сравнение по одной
доходности систематически выбирает то, что нельзя торговать с плечом.

### 01.09.2026 — два прогона, и оба обманывают, но в разные стороны

| прогон | лучший на проверке | «купи и держи» | в рынке |
|---|---|---|---|
| 9 монет | +2.085 | **+6.629** | 46% |
| 2 монеты | +0.151 | **−5.651** | 40% |

**На девяти монетах модель втрое ХУЖЕ простого удержания.** При этом
вердикт напечатал «преимущество переживает проверку» — замена логики
вердикта не применилась, бенчмарк только печатался. Дефект исправлен.

**На двух монетах она удержание обгоняет** — но лишь потому, что рынок там
падал, а модель сидела в позиции 40% времени. Быть вне падающего рынка не
значит уметь выбирать момент.

**Это одна и та же ошибка, зеркально отражённая.** На росте недоинвестиро-
ванность выглядит проигрышем, на падении — преимуществом. Ровно так
«альфа» недельного планнера перевернулась с +20 п.п. на −20, когда рынок
сменил направление, и я тогда принял её за качество решений.

**«Купи и держи» — негодный контроль.** Введён правильный:
`random_entry_r` — случайные входы с ТОЙ ЖЕ долей времени в рынке и той же
длительностью удержания. Он снимает и направление рынка, и экспозицию;
остаётся единственный вопрос — есть ли умение ВЫБИРАТЬ момент.

Вердикт теперь решается сравнением с этим контролем.

### 01.09.2026 — перебор дал «преимущество», и я его оспорил

Первый прогон: лучший набор (окно 60, удержание 20, нормировка, только
лонг) дал на обучении +0.941, на проверке **+2.053**; медиана по всем
конфигурациям на проверке +0.240.

Выглядит как успех. Три признака говорят, что это может быть не он:

- **проверка ЛУЧШЕ обучения.** У настоящего преимущества обычно наоборот;
  такое соотношение указывает, что проверочный период был благоприятнее;
- **все пять лидеров — только лонг с максимальным удержанием.** Длинное
  удержание плюс только лонг асимптотически превращается в «купи и держи»;
- **медиана по ВСЕМ конфигурациям положительна.** Если зарабатывают даже
  посредственные наборы, значит зарабатывал рынок.

Проверочный период — последние 270 дней, и мы знаем, что там было ралли:
HYPE +40% за неделю, у биткоина крупнейший недельный прирост за два года.

**Это та же ошибка, что с альфой недельного планнера:** направленная
стратегия на направленном рынке даёт прибыль без предсказания. Тогда я
принял бету за преимущество, и через две недели знак перевернулся.

Добавлены два числа, без которых результат перебора не значит ничего:
доходность «купи и держи» за тот же период и доля времени в позиции.
Вердикт теперь решается СРАВНЕНИЕМ С РЫНКОМ, а не знаком прибыли: если
модель не обгоняет «купи и держи» — это бета; если сидит в позиции больше
80% времени — обгон не доказателен.

### 01.09.2026 — H4 и перебор моделей моментума

**H4: слабый признак.** RS>0 n=158 avg R +0.041, RS<0 n=218 avg R −0.087,
разница **+0.128** при пороге 0.20. Направление подтвердилось — это было
записано 24.08 ДО данных, и лидеры действительно обогнали отстающих. Но
магнитуда ниже порога, а интервал для группы RS>0 приблизительно
[−0.070, +0.152] — ноль внутри. Экономически: фильтр по RS даёт около
12 сделок в месяц и **+0.25% депозита в месяц после издержек**, то есть
~3% годовых, неотличимых от нуля.

**Четвёртая дыра в предрегистрации:** для полосы 0.10–0.20 действие не
записано. Дописывать, увидев 0.128, — подгонка.

**Оператор справедливо возразил против преждевременного вывода.** Движок
есть, тысяча прогонов стоит минуты, а известные рабочие подходы мы не
перебирали. Внешний обзор дал четыре проверяемых утверждения:

- временнáя моментум-стратегия обгоняет кросс-секционную при высокой
  корреляции активов (31.96% годовых). У нас девять коррелированных альтов
  и отбор по RS — слабейший из двух вариантов;
- лучшее окно около 28 дней при удержании около 5 (Sharpe 1.51 против 0.84
  у рынка). У нас EMA50/200 — втрое длиннее;
- **доходность BTC выше ПОСЛЕ локальных максимумов**, а покупка у минимумов
  менее прибыльна. Наш фильтр перегрева блокирует вход у максимумов, то
  есть отсекает ровно то, что работает. Это независимо подтверждает
  наблюдение H4;
- волатильностная нормировка улучшает результат.

Заведён `src/momentum_sweep.py` и воркфлоу `momentum-sweep`: 140
конфигураций (окно 7–90, удержание 1–20, нормировка, только-лонг).

**Защита от подгонки встроена в процедуру.** История делится 70/30, лучший
выбирается ТОЛЬКО по обучающей части, судят по проверочной, которую он не
видел. Дополнительно печатается медиана по всем конфигурациям на проверке:
если хорош только победитель, а остальные нет — это подгонка, а не
преимущество. При переборе в сотню вариантов победитель находится и на
чистом шуме; вопрос лишь в том, переживёт ли он проверку.

### 01.09.2026 — варианты выхода: гипотеза опровергнута

| режим | n | avg R | WR | сумма R |
|---|---|---|---|---|
| **baseline** | 416 | **−0.023** | 46% | −9.6 |
| no_flip | 155 | −0.177 | 33% | −27.4 |
| trail | 141 | −0.221 | 28% | −31.2 |
| hybrid | 193 | −0.088 | 46% | −17.0 |

**Моя гипотеза была неверной.** Я предполагал, что выход по смене вердикта
режет победителей. Убрал его — win rate упал с 46% до 33%: этот выход не
обрезает прибыль, а СПАСАЕТ от убытков, закрывая сделки, которые иначе
дошли бы до стопа. Трейлинг оказался худшим из всех, ровно как
предупреждал источник: он усиливает имеющееся преимущество, но не создаёт
его.

**Расчёт «разрыв 0.17R» был арифметически верен и истолкован неверно.**
Формула `WR × 1.5 − (1−WR)` предполагает, что каждая сделка кончается
целью или стопом. У нас большинство кончается ни тем ни другим — выходом
около нуля. Это не потерянная прибыль, а несостоявшийся убыток.

**Вывод: дело не в выходах, дело во входах.** Три способа выхода на одних
сигналах, все хуже базового. Когда никакое правило выхода не помогает,
сигнал не содержит предсказания. Тактический слой входов преимущества не
имеет — не «пока не доказано», а проверено на 416 сделках.

Остаётся один непроверенный кандидат — H4 (относительная сила). Добавлен в
тот же прогон: каждая сделка размечается силой монеты против BTC НА МОМЕНТ
ВХОДА, сравнивается средний R у лидеров и отстающих. Порог из
предрегистрации 24.08 — разница 0.20R.

### 01.09.2026 — результат бэктеста и варианты выхода

**416 сделок за 700 дней: WR 46%, avg R −0.023, медиана −0.088, интервал
[−0.088, +0.049].** Выборка выше порога «убедительно». Верхняя граница
интервала — лучший случай, и он ДО издержек; комиссия с фандингом при
типичном стопе съедают около 0.02R. То есть потолок ценности слоя в
текущем виде — примерно ноль.

**Парадокс, который важнее вердикта.** При WR 46% и цели 1:1.5 ожидание
должно быть 0.46 × 1.5 − 0.54 = **+0.150R**. Фактически −0.023R. Разрыв
**0.17R на сделку** означает, что победители не доходят до цели — их
срезает выход по смене вердикта.

Внешняя практика подтверждает диагноз: «входы — 30% результата сделки,
выходы — 70%»; полный выход по фиксированной цели обрезает крупных
победителей до уровня обычных и разрушает асимметрию; сигнатура следования
тренду — WR 30–45% при победителях в 3–5 раз крупнее проигрышей. У нас
трендовые входы с выходами возврата к среднему.

**Я читал прежнее сравнение неверно.** Сопоставление «держим до SL/TP»
против «выход по политике» давало +0.3R в пользу политики, и я счёл это
доводом за неё. На выборке в 416 сделок видно, что политика режет и
убытки, и прибыль, а отрезанная прибыль перевешивает.

Заведены четыре варианта выхода на ОДНИХ И ТЕХ ЖЕ входах: `baseline`,
`no_flip` (без выхода по смене вердикта), `trail` (трейлинг по ATR вместо
цели, включается после 1R), `hybrid` (половина на 1R, остаток трейлингом).

**Две оговорки, записанные до результата:**
- трейлинг УСИЛИВАЕТ имеющееся преимущество, но не создаёт его: если входы
  теряют без него, никакое правило выхода их не спасёт;
- перебор вариантов на одних данных — множественное сравнение, склонное к
  подгонке. Результат прогона считается ГИПОТЕЗОЙ и требует подтверждения
  вне выборки, живой торговлей.

### 01.09.2026 — бэктест вместо ожидания

**Задача была поставлена неверно, и я это проглядел.** H2 дошла до порога
n=83: avg R +0.029, интервал [−0.074, +0.145], шум вчетверо больше сигнала.
Чтобы отличить эффект от нуля, нужно ~400 сделок для точности ±0.05 и
~1100 для ±0.03. При темпе 28 сделок в месяц это одиннадцать и тридцать два
месяца.

Внешняя практика: 100 сделок минимум, 200 убедительно, 500 сильная
значимость; при ожидании около нуля после 100 сделок нужен больший объём —
преимущество может быть очень малым. Зрелые проекты (freqtrade) получают
сотни сделок ОДНИМ прогоном по истории и используют живую торговлю уже как
проверку вне выборки. Мы делали наоборот: копили вперёд по 28 в месяц там,
где история отвечает за вечер.

**Выборка вдобавок смешанная.** Общее правило: если правила входа, выхода
или размера менялись внутри периода, это две разные стратегии в одном
наборе, и счёт начинают с текущей версии. Мы меняли логику 23.08 (H3),
значит 83 живые сделки — смесь.

Заведён `src/tactical_backtest.py` и воркфлоу `tactical-backtest`
(вручную). Прогоняется ТА ЖЕ функция вердикта, что в проде, по дневным
свечам; вход по закрытию свечи сигнала, стоп тем же `sl_for`, выход по
стопу, цели или встречной смене вердикта. При касании и стопа, и цели
внутри свечи консервативно засчитывается стоп.

**Чего в бэктесте НЕТ:** комиссий, фандинга, проскальзывания. Результат —
верхняя оценка. Если слой не окупается там, в живой торговле не окупится
тем более.

**Найдено попутно:** вердикт строит индикаторы из свечей вида
`{"o": c, "h": c, "l": c, "c": c}` — тот же дефект, что 31.08 нашли в
расчёте стопа, но глубже, в самой логике. ATR и swing-уровни внутри
вердикта занижены. Бэктест воспроизводит это как есть — он обязан мерить
развёрнутую логику, а не улучшенную. Вопрос вынесен отдельно.

### 31.08.2026 — стоп в дайджесте был вдвое уже настоящего

Два письма одного дня по одной монете:

```
тактический сигнал  HYPE · SL 72.68 (11.3%)
дайджест            HYPE · стоп 77.04 (5.9%)
```

Причина — мой код от 30.08: свечи для ATR собирались из одних закрытий,
`{"o": c, "h": c, "l": c, "c": c}`. При `high = low = close` истинный
диапазон схлопывается до |close − close_prev|, ATR занижается втрое, стоп
выходит вдвое уже. Настоящие свечи в этот момент уже загружены — их просто
отбрасывали при сборке данных дайджеста.

**Слишком узкий стоп опаснее отсутствующего.** Он выбивает позицию
рыночным шумом: оператор получает серию мелких убытков там, где сделка
была нормальной, и решает, что система не работает.

Свечи проведены в оба построителя данных дайджеста. Первая правка задела
только один — второй остался бы со старым стопом молча, поэтому добавлен
тест, требующий обоих. Это уже третий случай за два дня, когда правка «в
одном месте» оказывалась неполной (цены — семнадцать мест, свечи — два).

### 30.08.2026 — формат цен стал общим (и почему одной правки не хватило)

Утром я поправил `_fmt_price` в дайджесте и счёл цены вылеченными. Вечером
heartbeat в новом канале показал ту же поломку:

```
🟢 ASTER LONG · вход $1 · SL $1 · TP $1 · сейчас $1 (+6.0%)
```

Вход, стоп и цель — одно число. Причина: форматов по системе было
семнадцать, каждый со своим `:,.0f`, и правка одного места создала ложное
впечатление, что вылечено всё.

Формат вынесен в `src/money.py` и используется всеми. Разделены цены и
суммы: у счёта, нотионала китов и PnL в долларах целые уместны, точность
там не нужна. Сторож в тестах ругается только на цены — первая версия
регулярки ловила и суммы, то есть требовала бы бессмысленных дробей.

**Урок в копилку к §4:** «исправил в одном месте» и «исправил» — разные
вещи, когда одна величина форматируется в семнадцати местах.

### 30.08.2026 — точность цен и новизна сигнала

Разбор письма по UX выявил два пункта сверх уже сделанного.

**Цены — это был баг, а не оформление.** `NEAR $2` при реальной цене
$1.88: формат округлял всё в диапазоне 1–1000 до целого. Для BTC нормально,
для дешёвых монет — нет, а их четыре из девяти. При стопе в 1.6% ошибка
округления больше всего риска сделки: оператор не мог проверить, по той ли
цене входит. Письмо для этих монет оставалось неисполнимым даже после
того, как в него добавили стоп. Теперь значащая точность, а не
фиксированная.

**Новизна сигнала.** Читая сводку, оператор не мог понять: вход появился
сегодня или висит третий день. От этого зависит поведение — новый сигнал
повод действовать, тот же самый третий день повод не действовать, его уже
видели. Отметка «🆕» или «N-й день» рядом с вердиктом; состояние в
`digest_prev.json`, смена направления считается новой идеей.

**Не сделано намеренно:** предупреждение о бета-ставке по-прежнему стоит
после списка, «НЕ ВХОДИТЬ» набрано капсом, значки ⚪️ и ⚪ разнобойны. Это
оформление, оно ждёт конца моратория.

### 30.08.2026 — дайджест как руководство к действию

Три претензии оператора к письму, все справедливые: пять строк про то,
чего делать НЕ надо; по входам нет стопа, открыть сделку нельзя; четыре
входа без приоритета.

Сделано: «НЕ ВХОДИТЬ» сворачивается в строку-итог с причиной одним словом
(перегрев / слабый сигнал), по каждому входу печатается стоп и размер,
входы упорядочены по относительной силе.

**Оговорка по приоритету, важная.** Валидированного способа ранжировать
входы у системы НЕТ. Относительная сила — кандидат из гипотезы H4, которая
зарегистрирована, но не проверена. Поэтому рядом с каждым входом стоит само
число RS: оператор видит, на чём основан порядок, и может с ним не
согласиться. Отбор сигналов не меняется — измеримость H3/H4 не страдает.

**Preflight не пустил правку и вскрыл третий класс дефектов.** Три теста
читали БОЕВОЙ `state/` через жёсткие пути в коде: маркер дайджеста и
тактический журнал. Их результат зависел от того, что реально висит в
проде — пока незакрытых exit не было, зелено; появился висящий exit по BTC
— красно. Один из них ходил в сеть 55 секунд. Дефекты не мои и жили давно;
утром preflight был зелёным только потому, что маркер содержал сегодняшнюю
дату. Жёсткий путь в `daily_report` заменён модульной константой
`STATE_DIR`, три теста изолированы. Прогон набора: 60 с → 5.5 с.

**Превью вскрыло два дефекта, оба исправлены** (§7.7): стоп мог быть
напечатан не на своей стороне от входа, и размер не делился между
одновременными входами (пять сигналов по 50% депозита каждый).

### 28.08.2026 — виртуальные сделки: что уже есть и чего не хватало

Запрос оператора: «пусть программа делает виртуальные сделки по своей
системе». **Она их уже делает** — 97 входов и 74 закрытых с оценкой R,
независимо от того, держит ли оператор что-то реально (`notified: false`
при отсутствии живой позиции). Данные для H2 берутся именно оттуда.

Проверены две гипотезы о том, чего этим сделкам не хватает:

- **Фандинг не учитывается** — не подтвердилось. Медиана удержания 0.67
  дня, средняя цена фандинга −0.001 R, ни одна сделка не теряет на нём
  больше 10% риска. Гипотеза была моя, данные её отвергли.
- **Сделка не помнит, каким правилом сигнал пропущен** — подтвердилось и
  исправлено. Пометка `h3_unblocked` писалась только в журнал вердиктов;
  чтобы посчитать H3, приходилось сопоставлять два журнала по монете и
  дате. Теперь пометка едет во входную запись тактического журнала, где
  живут сделки с реальным R.

Ограничение, которое этим не лечится: H3 срабатывает редко (нужны
`TRANSITION` + медвежья фаза + сильный тренд), поэтому её выборка растёт
медленно независимо от объёма виртуальной торговли.

### 28.08.2026 — фикс подтверждён, каскад в детекторе убран

**Разделение сбора и окна работает.** Данные за 28.08 собраны в 01:14 UTC —
глубокой ночью, вне окна отправки. До правки этот прогон не собрал бы
ничего, как это и случилось 27.08.

**Каскад в письме детектора убран** (§7.6). Заодно на самом preflight
нашлись две вещи, обе — на пробах, а не в проде:

- поиск секретов находил **сам себя**: собственный шаблон в исходнике
  preflight блокировал пуш. Шаблон теперь собирается из кусков, свой файл
  исключён;
- поиск секретов **не видел новых файлов**: `git grep` без `--untracked`
  смотрит только отслеживаемое, а preflight запускается ДО `git add`.
  Токен в новом файле уехал бы первым же коммитом. Проверено подложенной
  пробой: до правки молчал, после — называет файл и строку.

Второе важнее первого: проверка, которая не видит целого класса файлов,
опаснее отсутствующей — на неё полагаются.

### 27.08.2026 — причина простоя найдена

Лог Actions, предоставленный оператором:

```
16:02 UTC  Run python -m src.daily_monitor
           No open positions — skipping report.
           no verdict journal changes to commit
```

Воркфлоу отработал штатно и не сделал ничего. 16:02 UTC — вне окна
06–14 UTC, поэтому не ушла сводка, **и вместе с ней не собрались данные**.
Из восьми слотов cron в окно попадали четыре; GitHub Actions при нагрузке
пропускает запуски по расписанию — выпали дневные слоты, и день наблюдений
пропал целиком.

**Корень не в окне, а в том, что к нему были привязаны две разные задачи.**
Окно существует ради оператора: сводка «что покупать сегодня» бессмысленна
ночью. У журнала такого ограничения нет — данные нужны всегда, годится
любой прогон дня.

Разведено: сбор идёт на ЛЮБОМ прогоне, сообщение уходит только в окне.
Пометки тоже две — `verdict_collection.json` и `digest_sent.json`: собранные
данные повторять не нужно, а не ушедшее сообщение стоит попробовать снова
на следующем прогоне внутри окна.

**Контракт сработал сразу.** Заведя вторую пометку, я снова не добавил её в
`git add` — третий раз та же ошибка. Тест из этой же сессии уронил прогон
до пуша, а не через две недели в проде.

### 27.08.2026 — откуда берутся дефекты (комплексная проверка)

Оператор: «постоянно вылезают какие-то проблемы». Проверка началась с
честного учёта, а не с поиска нового.

**Из 15 находок августа 6 внёс я сам** — 40%:

| дата | что | сколько прожило |
|---|---|---|
| 03.08 | `morning_digest.json` не коммитился | тот же день |
| 08.08 | «спокойно» гасилось без учёта стороны | 5 дней |
| 21.08 | `verdict_collection.json` не коммитился | 13 дней |
| 21.08 | детектор кричал на законную тишину | 18 дней |
| 24.08 | критерии отката H3 неизмеримы | 1 день |
| 27.08 | порог детектора не ловит пропуск суточного источника | 24 дня |

**Четыре из шести — в одном месте: воркфлоу и детектор.** Это была
единственная часть системы без тестов. Тысяча тестов покрывала `src/` и не
покрывала ни YAML, ни договор «код пишет файл — воркфлоу его коммитит».
Дважды подряд одна и та же ошибка с маркером, причём второй раз — через две
недели после того, как я записал это правило в §4 этого документа.

**Вывод: правило, которое нельзя соблюдать вниманием, надо проверять
машиной.** Заведён `tests/test_workflow_contract.py`: YAML разбирается,
триггеры на месте, cron из пяти полей, у каждой работы есть шаги, все точки
входа существуют, каждый файл состояния кем-то коммитится, ничего лишнего
не коммитится, шаги с `git commit` задают identity.

**Проверка проверки.** Первая версия теста ошибку НЕ поймала: она искала имя
файла где угодно в тексте воркфлоу и засчитывала упоминание в комментарии.
Тест ужесточён до поиска в `git add` и проверен воспроизведением реальной
поломки: на целом воркфлоу молчит, на сломанном называет ровно тот файл.
Тест, не проверенный на настоящем дефекте, — это ещё не проверка.

### 27.08.2026 — простой daily-monitor и калибровка детектора

Оператор: «сегодня был сильный рост по всем монетам, пришло только два
сообщения». Рост подтверждён внешними источниками: широкое ралли
альткоинов, HYPE около +40% за неделю, ZEC и LINK свыше +30%, у биткоина
крупнейший недельный прирост за два года.

**daily-monitor не писал с 26.08 07:41 — 37 часов.** Остальные воркфлоу
(tactical, guard, whale) в это время работали, коммиты есть. Cron, YAML,
структура шагов, переменные окружения и код проверены — целы; запуск
`python -m src.daily_monitor` в песочнице доходит до сетевых вызовов
штатно. Причина в логах GitHub Actions, доступа к которым нет.

**Что исправлено здесь** — не причина, а слепота к ней: порог свежести
стал зависеть от частоты источника (§4.4). Детектор на текущих данных
теперь поднимает тревогу.

### 24.08.2026 — субботний планнер в тени

Решение оператора: «на неделю не сделать чёткий прогноз, убрать». Результат
планнера +6.1% против +26.5% рынка за 11 недель.

**Диагноз, поставленный ДО отключения.** Последние три субботы планнер выдал
`EXIT`, `EXIT`, `SKIP` — все с `coin: None`, то есть кандидатов не перебирал
вовсе. Причина не в качестве недельного прогноза:

| суббота | режим | фаза | решение |
|---|---|---|---|
| 08.08 | BEAR | EARLY_BEAR | EXIT |
| 15.08 | BEAR | EARLY_BEAR | EXIT |
| 22.08 | TRANSITION | EARLY_BEAR | SKIP |

Это **тот же застрявший `EARLY_BEAR`**, который H3 починила 23.08 для
дневного слоя и не тронула в недельном. Планнер не ошибался в прогнозе — он
не делал прогноза, потому что его блокировали на входе.

**Что сделано:** тень, не удаление.
- Пост в канал был отключён ещё раньше (`WEEKLY_SILENT`); теперь переменная
  задана в воркфлоу явно, а не подразумевается — возврат без правки кода.
- Строка «Суббота: альфа …» убрана из недельной сводки. Она меряла не
  качество выбора, а степень инвестированности: за две недели знак
  перевернулся с +20.3 пп на −20.5 пп при НЕИЗМЕННОЙ доходности планнера
  +6.1% — изменился только рынок.
- Расчёт и запись `decisions.jsonl` продолжаются: «а если бы» остаётся
  измеримым.

**Почему не удалили код.** Метрика, по которой удаляли бы, меняет знак за
две недели, а причина плохого результата опознана и уже исправлена одним
слоем ниже. Удалять навсегда по нестабильному числу, зная про исправимую
поломку, — необратимое решение на слабом основании. Если оператор захочет
удалить совсем, это делается в одно действие и осознанно.

**Открытый вопрос:** применять ли логику H3 к недельному слою. Не делаем
сейчас — H3 живёт вторые сутки, и второе изменение в том же окне лишит оба
измеримости.

### Предзарегистрированная гипотеза H4 — фильтр перегрева инвертирует отбор

Зарегистрирована 24.08.2026, до сбора данных. **Логика НЕ меняется сейчас**
— причина ниже.

**Наблюдение.** Дайджест 24.08: все девять монет в восходящем тренде, пять
заблокированы «overbought — ждать pullback», четыре прошли. Прошли ровно
четыре самые слабые по относительной силе:

| заблокированы | RS 30д | | прошли LONG | RS 30д |
|---|---|---|---|---|
| ZEC | +52.2 | | TAO | +1.8 |
| MORPHO | +19.6 | | NEAR | −8.8 |
| HYPE | +16.7 | | ASTER | −10.2 |
| ETH | +10.8 | | ONDO | −21.4 |

Не единичный случай. На 954 записях с RS:

| группа | медиана RS 30д | n |
|---|---|---|
| заблокированы по overbought | **+7.5** | 52 |
| вердикт LONG | 0.0 | 143 |
| заблокированы по oversold | −2.4 | 154 |

**Гипотеза.** Фильтр перегрева на RSI — механизм возврата к среднему,
пришитый к трендовому ядру. Он систематически отсеивает лидеров и
пропускает отстающих: система покупает то, что не выросло, именно потому
что оно не выросло.

Это ровно та критика аналитика от 16.06, записанная в
`docs/analyst_feedback_june_16.md`: «гибрид следования тренду и возврата к
среднему; RS против BTC вероятно сильнее RSI/фандинга/китов для альтов».
Тогда решили не рефакторить, а собирать RS как наблюдаемость. Собрали.

**ПОЧЕМУ НЕ ПРАВИМ СЕГОДНЯ.** H3 внесена 23.08 и живёт первые сутки; её
критерии отката меряются на выборке, которая только началась. Изменить
фильтр перегрева сейчас — значит смешать два изменения в одном окне и
лишить оба измеримости. H4 ждёт закрытия H3.

**Что меряем при n≥40 закрытых сделок после закрытия H3:**

1. корреляция `rs_30d` на входе с фактическим R;
2. avg R сделок с `rs_30d > 0` против `rs_30d < 0`;
3. доля блокировок по overbought среди монет верхнего квартиля RS;
4. fwd-доходность заблокированных по overbought против прошедших LONG.

**Решающие правила, зафиксированные заранее:**

- avg R при `rs_30d > 0` выше, чем при `rs_30d < 0`, минимум на 0.20 →
  RS предсказывает, фильтр перегрева вредит: заменить веto по RSI на
  понижение размера, лидеров пропускать.
- разница по модулю < 0.10 → RS не предсказывает, фильтр не при чём,
  гипотезу закрыть.
- fwd-доходность заблокированных по overbought НИЖЕ, чем у прошедших →
  фильтр работает как задумано, закрыть.
- любой исход → RSI-порог сам по себе не трогаем: под вопросом право
  фильтра ветовать, а не его калибровка.

### 24.08.2026 — дыра в собственной предрегистрации

H3 сработала в бою: два LONG при `TRANSITION` + `EARLY_BEAR`. Проверка
показала, что критерии её отката **неизмеримы** — пометки для
разблокированных сигналов не было. Добавлена (§3). По всей истории таких
сигналов 47, из них 4 после правки; остальные 43 — июньские, когда режим
подолгу стоял в `TRANSITION`, и они дают материал для оценки задним числом.

### CTO-аудит 23.08.2026 — системные ограждения

Повод: проблемы находились **случайно** — по красному прогону, по времени
выполнения. Случайная находка означает, что проверки нет, поэтому аудит
закончился не списком правок, а ограждениями в `tests/conftest.py`.

**Что нашлось.**

1. **Тесты ходили в реальную сеть.** Два теста: один в API Hyperliquid,
   второй — в `api.telegram.org`. Второй хуже: с боевыми токенами в
   окружении прогон тестов писал бы оператору в канал.
2. **Тест дописывал в БОЕВОЙ журнал вердиктов** — четыре строки за прогон,
   в данные, по которым считаются чекпойнты. Причина: жёсткий путь к
   `state/` в обход `STATE_DIR`, который нельзя подменить.
3. **Дубли в журнале.** Монета, которую оператор держит, журналилась
   дважды за один прогон: путём позиции и путём дайджеста, с одинаковым
   `ts`. 14 пар с 03.08, по одной в день.

**Почему не замечали.** Боевой код ловит сетевые сбои широким `except` и
пишет warning. Незапатченный вызов не падал — он молча висел до таймаута и
давал зелёный прогон. Первый вариант стража это тоже не поймал: его
исключение съедал тот же `except`. Работающий вариант **копит попытки** и
валит тест на разборке фикстуры, куда продовый `except` не дотягивается.

**Что теперь стоит постоянно** (`tests/conftest.py`, autouse):

- сеть в тестах запрещена на уровне сокета; снимается маркером
  `@pytest.mark.network`;
- боевой `state/` сверяется до и после каждого теста — созданный или
  изменённый файл валит тест с указанием, что подменить.

**Побочный эффект:** прогон ускорился с 20 до 5.5 секунд — почти всё время
уходило на таймауты сетевых вызовов, которых не должно было быть.

**Проверено и чисто:** маркеры состояния все коммитятся; гейтов по
настенным часам не осталось; передискретизация прекратилась 21.08 (20–21.08
по 4 записи на монету в день, 22–23.08 — по одной); секретов в истории нет.

### Доделано 23.08 (хвост мелочей)

- **Страж достоверности ROI в KPI.** Отчёт с 03.08 не печатает процент,
  если счёт двигали вводами/выводами; в KPI того же стража не было, хотя
  строка выглядит и читается так же. Одно пополнение — и KPI показал бы
  процент, который отчёт в тот же день скрыл. Теперь `roi_is_reliable`
  применяется в обоих местах.
- **Окно дайджеста.** Cron `0 7-19/2` → `0 5-19/2`, нижняя граница окна
  07:00 → 06:00 UTC. Причина: Actions доставляет с задержкой 1–3 часа, и
  первый тик дня падал в 09–10 UTC, а сводка приходила в 12–13 МСК.
- **Ворота денег — не задача.** Закрытые ворота при неокупающейся тактике
  это работающий механизм, а не поломка. Записано в открытые вопросы,
  чтобы не путать с долгом.

### H3 — фаза не ветует сильный тренд (правка внесена 23.08.2026)

**Это изменение замороженной логики.** Внесено в день чекпойнта, решением
оператора, что политика §1 и допускает. Критерии отката записаны НИЖЕ, до
того как появились новые данные.

**Что меняется.** При неопределённом режиме (`TRANSITION`/`None`) фаза
больше не отменяет полноценный тренд (`|trend_score| == 2`, EMA50/EMA200
выстроены). Слабый сигнал (`|trend_score| == 1` — отскок в нисходящем
тренде, коррекция в восходящем) фаза ветует по-прежнему: там тренда нет, и
медленный цикловой слой добавляет настоящую информацию.

**Что НЕ меняется.** Вето режима абсолютно: `BEAR` запрещает лонг, `BULL`
запрещает шорт. Правка касается только случая, когда режим сам не знает
направления.

**Контрфакт на всей истории** (505 сырых направленных сигналов):
разблокировано **30** — из них 25 в окне роста 18–23.08 и 5 за все
предыдущие два месяца. Ни одного SHORT. Правка точечная, а не «снять
тормоза»: она чинит связку «медленная фаза × неопределённый режим», а не
инвариант иерархии.

**Критерии отката, зафиксированные до данных:**

- avg R по сигналам, прошедшим благодаря правке, < −0.30 при n≥15 →
  откатить, фаза при `TRANSITION` была права.
- доля таких сигналов > 40% от всех эмитированных → правка открыла шлюз,
  а не окно: поднять требование до подтверждения тренда двумя днями.
- `regime == TRANSITION` держится > 60% времени за 30 дней → проблема не
  в фазе, а в детекторе режима; правку оставить, но открыть отдельный
  вопрос по OracAI.
- ничего из перечисленного → оставить, вернуться при n≥40.

**Как считаются «сигналы, прошедшие благодаря правке» (добавлено 24.08).**
При регистрации 23.08 критерий был записан, но пометки для таких сигналов
не существовало — неизмеримый критерий это не критерий, а намерение.
Исправлено: `h3_unblocked(raw, final, regime, phase)` — сигнал прошёл
благодаря H3, если финальный вердикт совпал с сырым, а СТАРОЕ правило его
бы заблокировало. Определение точное, `trend_score` не требуется; флаг
пишется в журнал полем `h3_unblocked` и вычислим задним числом по всей
истории.

Первое срабатывание в бою — 24.08: ASTER и TAO, `TRANSITION` +
`EARLY_BEAR`, тренд вверх. Оба помечены «сила: низкая» — и это не
противоречие: тренд сильный (его и разблокировала H3), а совокупность
слабая, потому что фандинг +11% против лонга и китов по этим монетам нет.
Две шкалы меряют разное и обе честны.

**Цена, которую платим осознанно.** Логика вердикта изменена, значит
сравнение `verdict_raw` против `verdict` на объединённой выборке больше не
корректно. Для H2 и для сравнения слоёв выборка **начинается заново с
23.08**; прежние 505 наблюдений остаются как история старого правила.

### ИТОГ ЧЕКПОЙНТА 23.08.2026

Считано `scripts/h1_checkpoint.py` по порогам, зафиксированным до данных.

**H1 — дребезг режима: НЕ ПОДТВЕРЖДЕНА, закрыта.**
Доля `regime_flip` 20% при пороге 30% (n=25 на `policy_version=2`).
Возвратов внутри 24 ч — 0%. Медиана R по `regime_flip` +0.007.
Тревога 08.08 была поднята на n=7, где доля показывала 43%; на полной
выборке она осела втрое. Гипотеза закрыта, к ней не возвращаемся без новых
данных. Гистерезис остаётся как есть.

**H2 — окупаемость тактики: НЕ ДОКАЗАНО, возврат при n≥80.**
n=70, avg R +0.028, медиана −0.004, интервал [−0.087, +0.154] накрывает
ноль. Разрыв сторон LONG +0.088 (n=45) против SHORT −0.080 (n=25) = 0.168,
порог сужения был 0.20 — не сработал. Правило про сторону меряется по
журналу, как и записано в `compute_h2`; методика KPI дала бы 0.23 и
сработала. **Источник для правила H2 остаётся журнал** — менять его,
увидев, что другой даёт нужный ответ, значит отменить предрегистрацию.

**Главная находка дня — две метрики с одним именем.**

| | что мерит | avg R |
|---|---|---|
| KPI | держим сигнал до его SL/TP (горизонт 7д, цель +2R) | **−0.15** (n=86) |
| Чекпойнт | фактический R на выходе по политике | **+0.028** (n=70) |

Разные знаки. Сигналы, доведённые до собственных стопов, теряют; те же
сигналы, закрытые политикой выходов, выходят в ноль с плюсом. Это
свидетельство в пользу слоя выходов, а не против слоя входов: гвард режет
убыток раньше, чем до него дотягивается стоп. Подпись KPI исправлена, чтобы
величины больше не путались.

**Обе методики согласны в одном: SHORT хуже LONG.** KPI −0.29 против −0.06,
журнал −0.080 против +0.088. Разрыв ниже порога, но знак устойчив к смене
методики — к следующему чекпойнту это первый кандидат на сужение.

**Опровергнуто моё же утверждение от 09.08.** Я писал: «недельный слой
преимущество даёт, дневной нет», опираясь на альфу субботнего планнера
+20.3 п.п. На 23.08 та же альфа — **−20.5 п.п.**, при неизменных n=11
недель и неизменной доходности планнера +6.1%. Изменился только рынок:
−14.2% → +26.5%. То есть планнер не заработал ничего на движении в 40 п.п.,
а его «альфа» была функцией направления рынка, а не качества решений.
Недоинвестированность выглядит альфой на падении и провалом на росте.

### Наблюдение от 21.08 — цена инварианта на росте

**Это не предзарегистрированная гипотеза, а найденный постфактум факт.**
Записан отдельно и именно так помечен: подводить под него критерии задним
числом нельзя, решение принимает оператор на чекпойнте 23.08.

Три дня, 18→21.08, оператор вне рынка. Движение по нашим же меткам:

| HYPE | ZEC | ETH | ASTER | BTC | TAO | MORPHO | ONDO | NEAR |
|---|---|---|---|---|---|---|---|---|
| +29.5% | +26.9% | +25.1% | +22.9% | +20.4% | +17.7% | +17.1% | +15.2% | +15.1% |

Что говорила система за это окно (102 записи):

- сырой слой выдал **26 LONG**;
- до оператора дошло **3**;
- **23 погашено**, из них 19 при `regime=TRANSITION` с формулировкой
  «фаза EARLY_BEAR при неопределённом режиме — лонг не входить»;
- фаза оставалась `EARLY_BEAR` всё окно, пока цена росла на 20%+.

Гасит правило `direction_permission()`: при `regime != BULL` и медвежьей
фазе лонг запрещён. Оно же в июле спасало от контр-трендовых входов —
это та самая двусторонняя цена, которую мы сделали видимой 03.08.

**Что здесь под вопросом — не инвариант, а фазовый слой.** MVRV-фаза
двигается медленно по построению; связка «медленная фаза × неопределённый
режим» даёт блокировку, которая переживает целое движение. Вариант к
обсуждению: при `TRANSITION` доверять фазе только если она подтверждена
трендом, а не блокировать по ней одной.

**Требование закрыть не протухает.** Оно висит, пока позиция не закрыта или
пока не выпущен новый входной сигнал. Возраст показывается в строке.

**Осознанное отклонение** оформляется флагом, а не игнорированием:
Actions → `manual-hold` → монета, `set`/`clear`, причина. Строка меняется на
«ДЕРЖУ ПРОТИВ СИСТЕМЫ с датой и причиной», требование остаётся названным, и
такие сделки при разборе считаются отдельно.

---

## 4. Доктрина тихого отказа

**Сформулирована 03.08 после месячного простоя.** Мониторинг обязан отвечать
на вопрос «я ещё работаю правильно?», а не «я ещё говорю?».

Что произошло: 06.07 дайджест свернули в `daily-monitor` и повесили на
`now.hour == 7`. GitHub Actions ни разу не доставил этот cron внутри седьмого
часа — замеренные тики падают в 08–10 UTC. Ветка не отработала ни разу.
Четыре недели не собирались `verdict_raw` и `rs_30d/rs_90d`. Ошибок в логах
не было. Heartbeat молчал, потому что следит за молчанием канала, а канал
говорил каждые 2 часа. Healthcheck существовал, но не был подключён ни к
одному воркфлоу и, что хуже, не поймал бы: его проверки были вида «поле не
писалось НИКОГДА», а при 594 исторических записях такое условие не
срабатывает уже никогда.

**Правила, выведенные отсюда:**

1. **Настенные часы — не расписание.** Никаких `hour == N`. Условие
   формулируется как «первый прогон дня начиная с такого-то часа», с отметкой
   в state.
2. **Свежесть, а не покрытие.** Каждый поток данных — источник, поле, монета —
   поднимает тревогу сам, если ничего не произвёл за `STALE_HOURS` (36 ч).
   Доля покрытия ничего не говорит о том, жив ли поток.
3. **Состояние в Actions не переживает прогон.** Любой файл-маркер должен
   коммититься воркфлоу, иначе он теряется и гейт открывается на каждом тике.
4. **Порог свежести — по частоте источника, а не общий.** 27.08 не пришло
   ни дайджеста, ни отчёта: daily-monitor молчал 37 часов. Детектор
   промолчал — на его прогоне простой был 32 часа при общем пороге 36.
   Дыра структурная: дайджест суточный, пропуск ровно одного дня даёт
   24–30 часов, что ВСЕГДА меньше 36. Общий порог, сделанный под источник
   с шагом в два часа, не способен поймать пропуск дня у суточного.
   `SOURCE_STALE_HOURS` задаёт порог на источник; для суточных 30 часов =
   сутки плюс запас на задержку Actions.
5. **Сознательно отключённое молчание — не отказ.** `RETIRED_SOURCES`, чтобы
   детектор не кричал про то, что выключили нарочно.

Реализация: `src/journal_health.py`, воркфлоу `journal-health` раз в сутки,
Telegram только при проблеме.

---

## 5. Статистика: что считается наблюдением

**Одно наблюдение — одно независимое решение.** Монитор китов переотчитывает
стоящие позиции на каждом прогоне; все копии резолвятся по одним свечам и
падают вместе.

Замер 03.08 по ETH short: 104 сигнала от 6 китов за 5 дней, два кита дали 94
из них. Инфляция шла прямо в `n_events`, а это половина `is_actionable()`
(N≥10, WR≥60%) — один удачливый кит, переотчитанный одиннадцать раз, проходил
ворота выборки на одном наблюдении.

**То же и в журнале вердиктов.** 08.08: тихое журналирование при пустом
портфеле срабатывало на каждом тике — 40 записей вместо 8, пять наблюдений
одного суточного вердикта по каждой монете. Вердикт суточный, значит одно
наблюдение = монета + день. Маркер сбора (`verdict_collection.json`) отдельный
от маркера дайджеста: пустой портфель утром не должен лишать оператора
whitelist-блока, если позиции откроются днём.

Правило для китов: **кит + монета + сторона + день**, берётся самое раннее появление
(`dedupe_signals()`). После дедупа 203 → 31, ни одна группа не проходит N≥10.
Это и есть честная картина китового слоя.

---

## 6. Числа, которые нельзя показывать

**Не показывай число, за которое не можешь ответить.** Лучше пустое место,
чем «-703.2%».

- **ROI** печатается, только если стоимость счёта на начало периода
  действительно тот капитал, который заработал этот PnL.
  `net_flow = (end − start) − pnl`; если |flow| > 15% базы, база ≤ 0 или
  |roi| > 200% — процента нет, деньги остаются (`roi_is_reliable()`).
- **Эквити и нотионал** показываются вместе с плечом. Пара «$62» над
  позициями на $307 без множителя читается как противоречие.
- **Счётчик считает то, что показано.** «Всего сигналов: 146» при одном
  видимом входе: 145 из них — ротация лидерборда, не показываемая с 12.06.
  Заголовок описывал буфер, а не сообщение.
- **Что решено не показывать — не кладётся в буфер показа.** Скрытые
  правила копились в `whale_pending_info` неделями и мешали буферу
  чиститься: `render_digest` возвращает None, когда показывать нечего, и
  дайджест не уходил с 30.07 при интервале в сутки.
- **Одно число — один источник.** 09.08 в одном канале с разницей в шесть
  минут: KPI «счёт $143», отчёт «$130». KPI брал последнюю точку суточного
  ряда истории портфеля (сэмплируется, отстаёт), отчёт — живое значение из
  clearinghouse. Живое значение первично, история — только запасной путь.
- **Округлять в сторону, которая не убаюкивает.** Возраст требования
  считался через `delta.days`: 42 часа показывались как «1 дн назад».
  Занижать застой опаснее, чем завышать, — до двух суток показываем часы,
  дальше округляем к ближайшему.
- **Условие гашения = условие печати.** Строка «всё спокойно» гасилась по
  совпадению тикера, а «ЗАКРОЙ» печатается при совпадении тикера И стороны.
  Расхождение глушило отчёт из-за требования, которое не печаталось.

---

## 7. Язык интерфейса

1. **Термин из кода — не текст для оператора.** «Сырой вердикт» заменён на
   «график»: первое описывает стадию вычисления, второе называет источник
   сигнала.
2. **Один смысл — один значок.** 🔴 означал одновременно «нет стопа»,
   «вердикт SHORT» и «закрой по политике». Цвет оставлен за направлением,
   риск назван словами: `⛔ без стопа`.
3. **Говори действие, а не расхождение.** Строка сообщает не КТО с кем не
   согласен, а ЧТО делать:
   - `🔴 SHORT | Система рекомендует закрыть LONG` — система прямо против;
   - `⚪ WAIT | Система рекомендует подтянуть стоп: график развернулся вниз` —
     график против, режим сигнал погасил.
4. **Молчание при конфликте запрещено.** «✅ Без алертов, всё спокойно» не
   печатается, если по открытой монете висит требование закрыть.
5. **Второй элемент — только при разногласии.** Расхождение слоёв, никому не
   мешающее, не печатается вовсе.
6. **Называй причину, а не её последствия.** Письмо детектора 28.08 — 13
   предупреждений на один отказ: замолчал `whitelist_focus`, и следом
   отдельными строками отчитались два поля и все девять монет, которые он
   пишет. Детектор, печатающий экран текста на один отказ, перестают
   читать. Пока источник молчит, проверки по полям и монетам пропускаются:
   называется причина и один итог. Как только источники живы, поля и
   монеты снова проверяются поштучно — там это уже отдельные поломки.
7. **Письмо должно быть исполнимым.** По входу сразу даётся стоп и размер:
   без них оператору приходилось ждать отдельного тактического сигнала,
   чтобы открыть сделку. Два ограждения, добавленные 30.08 после превью:
   стоп обязан быть НА СВОЕЙ СТОРОНЕ от входа (стоп выше входа для лонга —
   не стоп, а мгновенный убыток; печатать неисполнимый план опаснее, чем
   не печатать ничего), и размер делится между одновременными входами —
   иначе предупреждение «дели размер» противоречит числу рядом с ним.
8. **Перечисляй отклонения, а не норму.** Замер 03.08: восемь одинаковых
   строк «НЕ ВХОДИТЬ» занимали 46% сообщения. Сплошной WAIT схлопывается в
   одну сводку со списком тикеров — видно, что все проверены. Появится
   вход — список разворачивается обратно: тогда важно сравнение. `NODATA`
   не прячется никогда: это отказ источника, а не отсутствие сигнала.
   Позиции без данных выпадают из строк-перечислений (киты).
7. **Один факт — одно место.** `regime · phase` печатался трижды в одном
   сообщении. Встроенный дайджест свою подпись не дублирует, режим живёт
   в подвале отчёта.
8. **Метка канала.** Сообщения HL живут в общем канале с Polymarket, и без
   метки два потока сливаются: и там и там таблички, доллары и эмодзи
   направления, а решения они требуют разных. Префикс ставится в `_send()` —
   единственной точке, через которую проходит всё исходящее. В билдерах его
   однажды забудут на новом типе сообщения. Менять — константа `HL_PREFIX`
   в `src/telegram_sender.py`, одна строка.
9. **Ночью не пингуем.** Окно утреннего дайджеста ограничено сверху
   (`LATEST_UTC_HOUR`). Пропущенный день лучше сводки «что покупать
   сегодня» в полночь: привычка к ночным пингам стоит дороже одной записи
   в журнал.

---

## 8. Инженерная дисциплина

- **TDD.** Тест пишется первым и должен упасть по делу.
- **Прямой push в main**, CI на каждый push. 850 тестов на 03.08.
- **Код возврата проверяется отдельной переменной, не через пайп.** 03.08
  коммит `d8e89c2` уехал с красным тестом: в цепочке `pytest | tail && git
  commit` код брался у `tail`, который всегда 0.
- **Токен в URL только на время push**, затем remote скрабится.
- **Характеризационные тесты** для модулей, которые предстоит измерять: они
  фиксируют поведение как есть, а не как должно быть.

---

## Изменения 03.08.2026

Тринадцать коммитов. Тактическая логика не тронута ни одним.

| коммит | что |
|---|---|
| `95a7238` | ROI не печатается при вводе/выводе средств; «всё спокойно» гасится при висящем ЗАКРОЙ |
| `d06916d` | нотионал и плечо в шапке; возраст висящего exit |
| `be4494e` | **утренний гейт**: `hour == 7` → первый прогон дня; флаг «держу против системы» |
| `cdfdc67` | детектор тихого отказа; 31 тест на `oracai`; дедуп китовых сигналов; косметика |
| `69f41ac` | сырой вердикт в отчёте, маркер позиции против системы |
| `b80c0f3` | «сырой» → «график» |
| `29f295c` | один элемент вместо двух |
| `d8e89c2` | `⛔ без стопа` вместо голого 🔴 |
| `9cc8d4c` | правка устаревшего утверждения (уехало красным, см. §8) |
| `9abc5d5` | рекомендация вместо описания расхождения |
| `035f41f` | коммит `state/morning_digest.json` — иначе дайджест улетал бы каждые 2 часа |
| `43bc454` `0d47cdc` | этот документ и протокол работы с ним |
| — | сжатие сообщения: сплошной WAIT в одну строку, киты без данных убраны, `regime` один раз; окно дайджеста ограничено сверху после ночной отправки в 23:26 |

### 21.08.2026

Три дефекта по жалобе «никаких предложений» — все инфраструктурные, логика
вердикта не тронута.

1. `verdict_collection.json` не коммитился воркфлоу: маркер терялся, сбор
   шёл на каждом тике окна — 36 записей в день вместо 9. Это правило §4.3,
   записанное мной 03.08 и нарушенное мной же 08.08.
2. Детектор кричал «источник daily_monitor молчит 2 дн», хотя тот пишет
   только по открытым позициям, а позиций не было. Введены
   `CONDITIONAL_SOURCES`: молчание такого источника — не отказ, пока жив
   хоть один безусловный. Ложная тревога обесценивает детектор быстрее
   пропущенной.
3. **Отменено решение оператора 13.06** «дайджест без позиций не слать».
   Три дня вне рынка при росте +15…+29% и ни одного предложения показали
   обратное: при открытых позициях сводка идёт подвалом отчёта, а без
   позиций отчёта нет вовсе. Теперь дайджест уходит и вне рынка, раз в
   сутки, в том же окне.

Плюс наблюдение о цене инварианта на росте (§3) — к решению на чекпойнте.

### 08.08.2026

Зарегистрирована гипотеза H2 об окупаемости тактического слоя (§3) — по
решению оператора, до сбора данных.

KPI и отчёт свели счёт к одному источнику; возраст требования больше не
округляется вниз.

**Вселенная расширена до 9 монет: добавлен ONDO** (решение оператора).
Задаётся двумя списками — `FOCUS_COINS` и `TACTICAL_COINS`; тест сторожит,
чтобы они не разъехались: именно расхождение однажды выкинуло ASTER из
датасета целиком. Настроек по отдельным монетам в проекте нет, кап плеча
берётся по исключению из `TIER1 = {BTC, ETH}`, поэтому ONDO — альт, 2×.

Для разбора 23.08: у ONDO будет две недели наблюдений против двух месяцев у
остальных. На статистику выходов это не влияет (считаем события, не монеты),
но при сравнении WR по монетам ONDO нужно исключать до набора истории.

Зарегистрирована гипотеза H1 о дребезге режима (§3) — до сбора данных,
с решающими правилами, зафиксированными заранее.

Сбор вердиктов при пустом портфеле — раз в сутки вместо каждого тика
(передискретизация, §5).

Метка `🟦 HL ·` на всех исходящих сообщениях: HL переехал в общий канал
с Polymarket.

Whale-дайджест: заголовок считает только показываемое; скрытые правила
больше не кладутся в буфер (он копился с 30.07 и мешал дайджесту уходить).
Заодно сведены условие гашения и условие печати строки «всё спокойно».

Накануне, 02.08: `f64f937` гистерезис K=3 и `policy_version=2`, `eca8cad`
закрытие `zone_strength` по её собственным воротам.

**Две ошибки в процессе, обе пойманы.** Заявил, что три монеты «структурно не
умеют сказать вниз», не посмотрев на сырой слой — неверно, опровергнуто
таблицей в §2. И запушил с красным тестом из-за пайпа — правило в §8.

---

### Сессия 13.08.2026

Зарегистрирована гипотеза **H3** — сузить универс до BTC (§3). Предложена
оператором из верного наблюдения: OracAI обучен на BTC, а его вердикт
применяется ко всем девяти монетам. Записана до данных, критерии
зафиксированы заранее.

Предложение содержало вторую часть — поднять плечо. Разделено: универс это
качество сигнала, плечо это размер риска. Плечо не трогается, пока H2 не
покажет окупаемость слоя. Отдельно: политика задаёт BTC/ETH 3×, факт на
13.08 — 5.2×, то есть текущий размер уже вне правила.

**Незакрытый выход.** Требование `exit (verdict_flip)` по ETH от 07.08 20:08
висит шестой день: позиция не закрыта и не помечена `manual-hold`. По §3
требование не протухает, а осознанное отклонение оформляется флагом. В
подвешенном состоянии сделка искажает статистику H1 — выход предписан, но
не исполнен, и результат непонятно как считать.

**Для H3 нужен срез по монетам** в `scripts/h1_checkpoint.py` — сейчас
скрипт агрегирует по всем. Добавить до 23.08.

---

## Открытые вопросы

- **Расписание дайджеста.** Первый тик с учётом задержки Actions ложится в
  09–10 UTC = 12–13 МСК, не 10:00. Чинится сдвигом cron `daily-monitor` с
  `0 7-19/2` на `0 5-19/2`, ценой одного лишнего сообщения в сутки. Не
  СДЕЛАНО 23.08: cron `0 5-19/2`, нижняя граница окна 06:00 UTC. Ценой
  одного лишнего прогона в сутки.
- **Плечо: оператор работает на 5× (заявлено 30.08.2026).** Прежняя запись
  «BTC/ETH 3×, альты 2×» была рекомендацией системы, а не фактом; теперь
  расхождение зафиксировано явно, и 5× перестал считаться нарушением.
  Настройка — `OPERATOR_LEVERAGE`.

  Что при этом НЕ меняется: стоп задаётся графиком (2·ATR или ближайший
  swing) и от плеча не зависит; размер считается от риска 1% депозита и
  дистанции стопа — тоже не от плеча.

  Что меняется по-настоящему — расстояние до ликвидации: при 5× это ~20%
  против позиции. Стоп шире НИКОГДА НЕ СРАБОТАЕТ: позицию вынесет раньше,
  и убыток будет не запланированным 1%, а всей маржой. Такой проверки в
  системе не было вовсе — при 2-3× она почти не срабатывала бы, при 5×
  существенна. Порог с запасом 20%, потому что биржа считает
  поддерживающую маржу и комиссии.
- **Ворота денег закрыты** (−0.07 при пороге +0.4). Это не дефект: пока
  тактика не окупается, ворота и не должны открываться — они ровно для
  этого. Но масштабирование заблокировано, и открыть их может только
  результат, а не правка. Держим в виду, не «чиним».
- **Разбор 23.08** — гистерезис при n≥20. По RS и raw-вердиктам к этому
  сроку будет три недели данных, а не месяц: сбор возобновляется 04.08.
