"""Дайджест и мгновенные алерты — одним письмом (14.09.2026).

Претензия оператора: писем слишком много. За 14 сентября пришло шесть, и
два из них — в 06:15 и 06:16, с разницей в МИНУТУ: сначала дайджест китов,
следом отдельным письмом CLUSTER ZEC.

Причина структурная: дайджест отправлялся в НАЧАЛЕ прогона (чтобы уходить
и в тихие дни), а мгновенные алерты — в конце того же прогона.

Теперь оба блока склеиваются в одно письмо. Запасной путь сохранён: если
прогон выходит рано — нет новых заполнений или кандидатов, — дайджест
уходит один, потому что он отчитывается за сутки, а не за прогон.
"""
import inspect

from src import whale_monitor


def test_digest_and_alerts_are_joined():
    src = inspect.getsource(whale_monitor)
    assert '"\\n\\n".join(parts)' in src, "блоки обязаны склеиваться"


def test_digest_still_sent_on_quiet_days():
    """Ранние выходы не должны глотать дайджест: он за сутки, не за прогон."""
    src = inspect.getsource(whale_monitor)
    assert src.count("_flush_digest_alone()") >= 3, (
        "запасной путь должен стоять перед каждым ранним выходом")


def test_no_separate_instant_send_remains():
    """Отдельной отправки алертов больше быть не должно — иначе вернётся
    та же пара писем с разницей в минуту."""
    src = inspect.getsource(whale_monitor)
    assert "sent instant alert with" not in src
